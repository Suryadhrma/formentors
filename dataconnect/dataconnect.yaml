specVersion: "v1beta"
serviceId: "formentors"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "formentors-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
